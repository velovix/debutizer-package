distributions:
  - bionic
  - focal
  - hirsute

upstream_repo: http://apt.debutizer.dev
upstream_components: [main]
upstream_is_trusted: true

upload_target:
  type: s3
  endpoint: https://storage.googleapis.com
  bucket: apt-debutizer-dev
  sign: true
